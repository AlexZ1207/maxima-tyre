name: GitHub Pages (disabled)

# The live site is hosted on Vercel. Keep this workflow only for a manual
# reminder so pushes to main do not publish the old static Pages site.

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Live site is on Vercel. This GitHub Pages workflow is disabled."
